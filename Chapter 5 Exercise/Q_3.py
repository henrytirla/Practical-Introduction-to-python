"""Write a program that asks the user to enter a value n, and then computes (1+ 1
2 + 1
3 +  + 1
n )􀀀
ln(n). The ln function is log in the math module."""

import math

# n = eval(input("Enter value of n"))
